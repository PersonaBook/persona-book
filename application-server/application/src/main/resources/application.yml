spring:
  application:
    name: application  # ?????? ?? (Actuator ??? ??? ??)

  thymeleaf:
    prefix: classpath:/templates/  # ? ??? ??
    suffix: .html                  # ??? ?? ???
    cache: false                   # ?? ??? ?? ???? (??? ?? ?? ??)

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL JDBC ????
    url: ${SPRING_DATASOURCE_URL}                # DB ?? URL (docker-compose?? ???)
    username: ${SPRING_DATASOURCE_USERNAME}      # DB ????
    password: ${SPRING_DATASOURCE_PASSWORD}      # DB ????

  jpa:
    hibernate:
      ddl-auto: update               # ?????? ?? ? ??? ?? ?? ?? ??
    show-sql: true                   # ???? SQL ?? ??
    properties:
      hibernate:
        format_sql: true            # SQL? ????? ??
        dialect: org.hibernate.dialect.MySQL8Dialect  # MySQL 8.x ?? ?? ??
        jdbc:
          time_zone: Asia/Seoul     # JDBC ?? ? ??? ?? (DB ??/?? ?? ??? ??)

  jackson:
    time-zone: Asia/Seoul           # JSON ???/???? ? ??? ???

  main:
    allow-bean-definition-overriding: true  # ?? ??? Bean? ??? ?? ???? ?? (??? ???? ??)
